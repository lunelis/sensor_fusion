%--- Erlang Inet Configuration -------------------------------------------------

% Add hosts
{host, {X,X,X,X}, ["hostname_computer"]}.
{host, {X,X,X,X}, ["nav_1"]}.
{host, {X,X,X,X}, ["sonar_1"]}.

% Do not monitor the hosts file
{hosts_file, ""}.

% Disable caching
{cache_size, 0}.

% Specify lookup method
{lookup, [file, dns]}.